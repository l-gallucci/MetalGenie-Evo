name: metalgenie-evo
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.8
  - hmmer>=3.3
  - prodigal>=2.6.3
  - samtools>=1.10   # optional: only needed for --bam
  - pip
  - pip:
    - -e .           # installs MetalGenie-Evo as an editable package
